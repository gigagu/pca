---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: gdptenants.gdp.standardchartered.com
spec:
  group: gdp.standardchartered.com
  names:
    kind: GDPTenant
    listKind: GDPTenantList
    plural: gdptenants
    singular: gdptenant
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: GDPTenant is the Schema for the gdptenants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GDPTenant
            properties:
              bc:
                type: string
              hcv-secret-account:
                type: string
              ibs:
                type: boolean
              itam:
                type: integer
              itam-name:
                type: string
              ito-unit:
                type: string
              jobs-queue:
                properties:
                  cpu:
                    type: string
                  memory:
                    type: string
                required:
                - cpu
                - memory
                type: object
              lob:
                type: string
              namespace:
                type: string
              object-store-buckets:
                description: LimitRanges    LimitRanges    `json:"limit-ranges,omitempty"`
                items:
                  properties:
                    bucket-size:
                      type: string
                    location:
                      type: string
                    name:
                      type: string
                  required:
                  - bucket-size
                  - location
                  - name
                  type: object
                type: array
              product:
                description: foo is an example field of GDPTenant. Edit gdptenant_types.go
                  to remove/update
                type: string
              ranger-policies:
                items:
                  properties:
                    schema_admin:
                      items:
                        type: string
                      type: array
                    schema_name:
                      type: string
                    schema_path:
                      type: string
                    schema_viewer:
                      items:
                        type: string
                      type: array
                  required:
                  - schema_admin
                  - schema_name
                  - schema_path
                  - schema_viewer
                  type: object
                type: array
              resilience-level:
                type: string
              resource-quotas:
                properties:
                  cpu:
                    type: string
                  memory:
                    type: string
                required:
                - cpu
                - memory
                type: object
              sbia:
                type: integer
              target:
                items:
                  type: string
                type: array
              tenant:
                type: string
              tenant-desc:
                type: string
              tenant-interactive-owner:
                items:
                  type: string
                type: array
              tenant-interactive-viewer:
                items:
                  type: string
                type: array
              tenant-noninteractive-owner:
                items:
                  type: string
                type: array
              tenant-noninteractive-viewer:
                items:
                  type: string
                type: array
              tenant-short-name:
                type: string
            required:
            - bc
            - ibs
            - itam
            - itam-name
            - ito-unit
            - lob
            - namespace
            - resilience-level
            - resource-quotas
            - sbia
            - target
            - tenant
            - tenant-desc
            - tenant-interactive-owner
            - tenant-interactive-viewer
            - tenant-noninteractive-owner
            - tenant-noninteractive-viewer
            - tenant-short-name
            type: object
          status:
            description: status defines the observed state of GDPTenant
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the GDPTenant resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              namespace:
                type: string
              ranger:
                items:
                  type: string
                type: array
              trino:
                items:
                  type: string
                type: array
              yunikorn:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
